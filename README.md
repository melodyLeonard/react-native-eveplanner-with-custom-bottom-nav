# react-native-eveplanner-with-custom-bottom-nav

1.  cd into the project directory (react-native-eveplanner-with-custom-bottom-nav)

2.  run:

    yarn start or npm start

3.  scan the QR code with an expo client.
